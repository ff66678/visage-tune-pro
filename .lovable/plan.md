

## Plan: 推荐卡片改为竖卡布局

### 改动文件
**`src/components/HomePage.tsx`** — 重写推荐区域卡片

### 布局思路
保持现有设计风格，但将卡片从"图片全覆盖+底部文字叠加"改为"上图下文"的竖卡布局：

- 卡片宽度 `min-w-[200px]`，水平滚动，一屏显示约 2 张多
- 上方：圆角图片区域，约 `h-[140px]`，左下角保留现有 tag 标签
- 下方：`bg-surface` 信息区，显示标题、难度/时长等信息
- 容器 `gap-4`，Skeleton 同步更新

### 具体改动
1. 推荐卡片区域（约第 131-151 行）替换为上图下文结构
2. 图片区用 `rounded-t-2xl`，信息区用 `rounded-b-2xl`
3. 信息区包含：课程标题（`font-semibold`）、难度 + 时长一行
4. Skeleton 更新为匹配新高度
5. 从 lucide-react 引入 `Clock` 图标用于时长显示

