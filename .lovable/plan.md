

## Plan: 课程库页面视觉升级 + 功能丰富化

### 改动文件
**`src/components/LibraryPage.tsx`**

### 视觉升级

1. **顶部 Banner 区域**：在筛选标签下方添加一个精选课程横幅（取第一个 featured 课程），全宽大图 + 渐变遮罩 + 标题 + "立即开始"按钮，让页面顶部更有冲击力

2. **卡片样式升级**
   - 圆角加大 `rounded-2xl`，添加按压反馈 `active:scale-[0.97]`
   - 难度标签带颜色区分（初级绿、中级橙、高级红）
   - 右上角显示收藏心形按钮（已收藏=实心红，未收藏=空心），点击可收藏/取消
   - 如有 rating 数据，底部显示星标评分

3. **每个分类第一个课程做大卡片**
   - 占满宽度，高度 180px，底部渐变叠加标题信息
   - 其余课程保持 2 列网格

4. **分类标签升级**
   - 添加小 emoji 图标：👁 眼部、🦴 下颌、✨ 脸颊、💆 颈部
   - 选中态用渐变背景 + 底部小圆点

5. **分类标题装饰**
   - 左侧加 3px 彩色竖线
   - "查看全部"加 `ChevronRight` 箭头图标

### 新增功能模块

6. **收藏功能集成**：利用已有的 `useFavoriteIds` 和 `useToggleFavorite`，在每个卡片右上角添加收藏按钮

7. **热门排行横向滚动**：在 Banner 下方添加"热门课程"横向滚动条（取 rating 最高的 5 个课程），小圆角卡片 + 排名序号

8. **搜索栏升级**：圆角 `rounded-2xl`，右侧加筛选图标按钮（可展开排序选项：按评分、按时长、按难度）

### 不变的部分
- 数据源 `useCourses`、路由跳转、筛选逻辑保持不变
- 仅改视觉层 + 新增收藏交互和热门排行

